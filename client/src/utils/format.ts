export const money=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"});export const dateBR=(v:string)=>new Intl.DateTimeFormat("pt-BR",{timeZone:"UTC"}).format(new Date(`${v}T00:00:00Z`));export const typeLabel={income:"Receita",expense:"Despesa"} as const;export const statusLabel={paid:"Pago",pending:"Pendente",cancelled:"Cancelado"} as const;
